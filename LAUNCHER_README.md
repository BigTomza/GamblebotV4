# Gamblebot Launcher

Double-click `LAUNCH_GAMBLEBOT.bat` on Windows or run `python launch_gamblebot.py` to start the local server. The launcher opens your default browser at the application page. Use the Settings tab to enter your API key and select region and sport.
